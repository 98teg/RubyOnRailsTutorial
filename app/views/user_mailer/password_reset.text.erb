Para resetear la contrasena clique por aqui:

<%= edit_password_reset_url(@user.reset_token, email: @user.email) %>

Este link expirara en un par de horas.

Si no quieres formatear tu contrasena, ignora esto.
