Para resetear la clave de seguridad haga click en el siguiente link:

<%= edit_password_reset_url(@user.reset_token, email: @user.email) %>

Este link es valido durante las siguientes dos horas.

Ignore este correo si no desea cambiar su clave de seguridad.
